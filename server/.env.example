# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Postgres (Prisma)
# If using docker-compose.yml in repo root, this works:
DATABASE_URL=postgresql://taskflow:taskflow@localhost:5432/taskflow?schema=public

# Auth (required)
JWT_SECRET=dev_secret_change_me

# App base URL used in verification links
APP_BASE_URL=http://localhost:5173

# Admin bootstrap (optional override)
ADMIN_EMAIL=abdullahzarif050@gmail.com
ADMIN_PASSWORD=Zariffatiha11

# Optional SMTP (if unset, emails are logged to console)
# EMAIL_FROM=no-reply@yourdomain.com
# SMTP_HOST=smtp.yourdomain.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_password
